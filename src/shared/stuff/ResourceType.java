package shared.stuff;

/**
 * This enum is used to allow different source/output types, it covers some
 * common ones along with a custom option
 */
public enum ResourceType {
  FILE, DATABASE, OBJECT_STORE, STREAM, CUSTOM
}
